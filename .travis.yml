sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t kingsleye62/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kingsleye62/docker-react npm run test -- --coverage

